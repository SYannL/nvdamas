# Optional dependencies for mas_memory=memrl (MemRL / MemOS episodic memory).
# Install: pip install -r requirements-memrl.txt
#
# Pinned loosely to match baselines/MemRL; resolve conflicts with your env as needed.

memoryos>=1.0.0,<2.0.0
# Pin: MemOS sentence chunker API matches chonkie 1.2.x (newer chonkie breaks tokenizer_or_token_counter).
chonkie>=1.2.1,<1.3.0
qdrant-client>=1.9.0
tenacity>=8.0.0
tiktoken>=0.7.0
tqdm>=4.66.0
